numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
# Optional extras (install with: pip install -e ".[sc,llm,dev]")
#   sc  : anndata, scanpy, leidenalg, igraph   -> real .h5ad + Leiden clustering
#   llm : anthropic                            -> Claude-written narrative
#   dev : pytest                               -> test suite
